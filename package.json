{
  "name": "agent-factory-smallville",
  "version": "1.0.0",
  "description": "Real-time pixel-art dashboard for AI agents",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "build:all": "npm run build --workspace=shared && npm run build --workspace=client",
    "deploy": "node deploy.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.0.0"
  }
}